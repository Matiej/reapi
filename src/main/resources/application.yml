spring:
  application:
    name: @project.name@
  output:
    ansi:
      enabled: always
  data:
    mongodb:
      host: localhost
      port: 27018
      database: reapi_mongo_db
      auto-index-creation: true

app:
  version: @project.version@
  name: @project.name@

server:
  port: 8100